#!/usr/bin/env python

# simple hack to make this work
import sys
sys.path.append('./src')

from yabt import Options, YABT

if __name__ == "__main__" :
    options = Options()
    yabt = YABT(options)
    yabt.run()

